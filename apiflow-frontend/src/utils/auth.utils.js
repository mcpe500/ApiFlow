function isLoggedIn() {
  // Add your logic here to check if the user is logged in
  // For example, you can check if a token is present in local storage or if a session is active
  // Return true if the user is logged in, otherwise return false
}

module.exports = {
  isLoggedIn,
};
